name: tests

on:
  push:
    branches:
      - devops


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cd auth && npm install && npm run testhm --forceExit
